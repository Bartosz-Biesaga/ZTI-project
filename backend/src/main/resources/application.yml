spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hireme
    username: hireme
    password: hireme
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration
